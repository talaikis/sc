import React from 'react'

import Header from '../../components/Header'
import Sidebar from '../../components/Sidebar'

export default () => (
  <div className='container'>
    <Header />
    <Sidebar />
    <p>Here goes checkout, we have an API for it :-D</p>
  </div>
)
